interface:
  display_name: "Chany's Environment Preflight"
  short_description: "영상·이미지 로컬 도구와 한글 폰트 실행 환경 점검"
  default_prompt: "$chany-preflight로 이번 영상 조립이나 프레임 추출에 필요한 로컬 도구와 한글 폰트를 읽기 전용으로 점검하고, 누락된 항목의 안전한 해결안을 알려줘."
policy:
  allow_implicit_invocation: true
